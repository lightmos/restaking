swagger: '2.0'
info:
  title: Cosmos SDK - gRPC Gateway docs
  description: A REST interface for state queries
  version: 1.0.0
paths:
  /lightmos/restaking/buy_order_book:
    get:
      operationId: BuyOrderBookAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              buyOrderBook:
                type: array
                items:
                  type: object
                  properties:
                    index:
                      type: string
                    amountDenom:
                      type: string
                    priceDenom:
                      type: string
                    book:
                      type: object
                      properties:
                        idCount:
                          type: integer
                          format: int32
                        orders:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: integer
                                format: int32
                              creator:
                                type: string
                              amount:
                                type: integer
                                format: int32
                              price:
                                type: integer
                                format: int32
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /lightmos/restaking/buy_order_book/{index}:
    get:
      summary: Queries a list of BuyOrderBook items.
      operationId: BuyOrderBook
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              buyOrderBook:
                type: object
                properties:
                  index:
                    type: string
                  amountDenom:
                    type: string
                  priceDenom:
                    type: string
                  book:
                    type: object
                    properties:
                      idCount:
                        type: integer
                        format: int32
                      orders:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              format: int32
                            creator:
                              type: string
                            amount:
                              type: integer
                              format: int32
                            price:
                              type: integer
                              format: int32
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: index
          in: path
          required: true
          type: string
      tags:
        - Query
  /lightmos/restaking/denom_trace:
    get:
      operationId: DenomTraceAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              denomTrace:
                type: array
                items:
                  type: object
                  properties:
                    index:
                      type: string
                    port:
                      type: string
                    channel:
                      type: string
                    origin:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /lightmos/restaking/denom_trace/{index}:
    get:
      summary: Queries a list of DenomTrace items.
      operationId: DenomTrace
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              denomTrace:
                type: object
                properties:
                  index:
                    type: string
                  port:
                    type: string
                  channel:
                    type: string
                  origin:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: index
          in: path
          required: true
          type: string
      tags:
        - Query
  /lightmos/restaking/params:
    get:
      summary: Parameters queries the parameters of the module.
      operationId: RestakingParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params holds all the parameters of this module.
                type: object
            description: >-
              QueryParamsResponse is response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      tags:
        - Query
  /lightmos/restaking/sell_order_book:
    get:
      operationId: SellOrderBookAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              sellOrderBook:
                type: array
                items:
                  type: object
                  properties:
                    index:
                      type: string
                    amountDenom:
                      type: string
                    priceDenom:
                      type: string
                    book:
                      type: object
                      properties:
                        idCount:
                          type: integer
                          format: int32
                        orders:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: integer
                                format: int32
                              creator:
                                type: string
                              amount:
                                type: integer
                                format: int32
                              price:
                                type: integer
                                format: int32
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /lightmos/restaking/sell_order_book/{index}:
    get:
      summary: Queries a list of SellOrderBook items.
      operationId: SellOrderBook
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              sellOrderBook:
                type: object
                properties:
                  index:
                    type: string
                  amountDenom:
                    type: string
                  priceDenom:
                    type: string
                  book:
                    type: object
                    properties:
                      idCount:
                        type: integer
                        format: int32
                      orders:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              format: int32
                            creator:
                              type: string
                            amount:
                              type: integer
                              format: int32
                            price:
                              type: integer
                              format: int32
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: index
          in: path
          required: true
          type: string
      tags:
        - Query
  /lightmos/restaking/validator_token:
    get:
      operationId: ValidatorTokenAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ValidatorToken:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    total:
                      type: string
                    retire:
                      type: string
                    available:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /lightmos/restaking/validator_token/{address}:
    get:
      summary: Queries a list of ValidatorToken items.
      operationId: ValidatorToken
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ValidatorToken:
                type: object
                properties:
                  address:
                    type: string
                  total:
                    type: string
                  retire:
                    type: string
                  available:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              error:
                type: string
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    type_url:
                      type: string
                    value:
                      type: string
                      format: byte
      parameters:
        - name: address
          in: path
          required: true
          type: string
      tags:
        - Query
definitions:
  cosmos.base.query.v1beta1.PageRequest:
    type: object
    properties:
      key:
        type: string
        format: byte
        description: |-
          key is a value returned in PageResponse.next_key to begin
          querying the next page most efficiently. Only one of offset or key
          should be set.
      offset:
        type: string
        format: uint64
        description: |-
          offset is a numeric offset that can be used when key is unavailable.
          It is less efficient than using key. Only one of offset or key should
          be set.
      limit:
        type: string
        format: uint64
        description: >-
          limit is the total number of results to be returned in the result
          page.

          If left empty it will default to a value to be set by each app.
      count_total:
        type: boolean
        description: >-
          count_total is set to true  to indicate that the result set should
          include

          a count of the total number of items available for pagination in UIs.

          count_total is only respected when offset is used. It is ignored when
          key

          is set.
      reverse:
        type: boolean
        description: >-
          reverse is set to true if results are to be returned in the descending
          order.


          Since: cosmos-sdk 0.43
    description: |-
      message SomeRequest {
               Foo some_parameter = 1;
               PageRequest pagination = 2;
       }
    title: |-
      PageRequest is to be embedded in gRPC request messages for efficient
      pagination. Ex:
  cosmos.base.query.v1beta1.PageResponse:
    type: object
    properties:
      next_key:
        type: string
        format: byte
        description: |-
          next_key is the key to be passed to PageRequest.key to
          query the next page most efficiently. It will be empty if
          there are no more results.
      total:
        type: string
        format: uint64
        title: |-
          total is total number of results available if PageRequest.count_total
          was set, its value is undefined otherwise
    description: |-
      PageResponse is to be embedded in gRPC response messages where the
      corresponding request message has used PageRequest.

       message SomeResponse {
               repeated Bar results = 1;
               PageResponse page = 2;
       }
  google.protobuf.Any:
    type: object
    properties:
      type_url:
        type: string
      value:
        type: string
        format: byte
  grpc.gateway.runtime.Error:
    type: object
    properties:
      error:
        type: string
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          properties:
            type_url:
              type: string
            value:
              type: string
              format: byte
  lightmos.restaking.BuyOrderBook:
    type: object
    properties:
      index:
        type: string
      amountDenom:
        type: string
      priceDenom:
        type: string
      book:
        type: object
        properties:
          idCount:
            type: integer
            format: int32
          orders:
            type: array
            items:
              type: object
              properties:
                id:
                  type: integer
                  format: int32
                creator:
                  type: string
                amount:
                  type: integer
                  format: int32
                price:
                  type: integer
                  format: int32
  lightmos.restaking.DenomTrace:
    type: object
    properties:
      index:
        type: string
      port:
        type: string
      channel:
        type: string
      origin:
        type: string
  lightmos.restaking.Order:
    type: object
    properties:
      id:
        type: integer
        format: int32
      creator:
        type: string
      amount:
        type: integer
        format: int32
      price:
        type: integer
        format: int32
  lightmos.restaking.OrderBook:
    type: object
    properties:
      idCount:
        type: integer
        format: int32
      orders:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
              format: int32
            creator:
              type: string
            amount:
              type: integer
              format: int32
            price:
              type: integer
              format: int32
  lightmos.restaking.Params:
    type: object
    description: Params defines the parameters for the module.
  lightmos.restaking.QueryAllBuyOrderBookResponse:
    type: object
    properties:
      buyOrderBook:
        type: array
        items:
          type: object
          properties:
            index:
              type: string
            amountDenom:
              type: string
            priceDenom:
              type: string
            book:
              type: object
              properties:
                idCount:
                  type: integer
                  format: int32
                orders:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: integer
                        format: int32
                      creator:
                        type: string
                      amount:
                        type: integer
                        format: int32
                      price:
                        type: integer
                        format: int32
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  lightmos.restaking.QueryAllDenomTraceResponse:
    type: object
    properties:
      denomTrace:
        type: array
        items:
          type: object
          properties:
            index:
              type: string
            port:
              type: string
            channel:
              type: string
            origin:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  lightmos.restaking.QueryAllSellOrderBookResponse:
    type: object
    properties:
      sellOrderBook:
        type: array
        items:
          type: object
          properties:
            index:
              type: string
            amountDenom:
              type: string
            priceDenom:
              type: string
            book:
              type: object
              properties:
                idCount:
                  type: integer
                  format: int32
                orders:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: integer
                        format: int32
                      creator:
                        type: string
                      amount:
                        type: integer
                        format: int32
                      price:
                        type: integer
                        format: int32
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  lightmos.restaking.QueryAllValidatorTokenResponse:
    type: object
    properties:
      ValidatorToken:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            total:
              type: string
            retire:
              type: string
            available:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  lightmos.restaking.QueryGetBuyOrderBookResponse:
    type: object
    properties:
      buyOrderBook:
        type: object
        properties:
          index:
            type: string
          amountDenom:
            type: string
          priceDenom:
            type: string
          book:
            type: object
            properties:
              idCount:
                type: integer
                format: int32
              orders:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int32
                    creator:
                      type: string
                    amount:
                      type: integer
                      format: int32
                    price:
                      type: integer
                      format: int32
  lightmos.restaking.QueryGetDenomTraceResponse:
    type: object
    properties:
      denomTrace:
        type: object
        properties:
          index:
            type: string
          port:
            type: string
          channel:
            type: string
          origin:
            type: string
  lightmos.restaking.QueryGetSellOrderBookResponse:
    type: object
    properties:
      sellOrderBook:
        type: object
        properties:
          index:
            type: string
          amountDenom:
            type: string
          priceDenom:
            type: string
          book:
            type: object
            properties:
              idCount:
                type: integer
                format: int32
              orders:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int32
                    creator:
                      type: string
                    amount:
                      type: integer
                      format: int32
                    price:
                      type: integer
                      format: int32
  lightmos.restaking.QueryGetValidatorTokenResponse:
    type: object
    properties:
      ValidatorToken:
        type: object
        properties:
          address:
            type: string
          total:
            type: string
          retire:
            type: string
          available:
            type: string
  lightmos.restaking.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params holds all the parameters of this module.
        type: object
    description: QueryParamsResponse is response type for the Query/Params RPC method.
  lightmos.restaking.SellOrderBook:
    type: object
    properties:
      index:
        type: string
      amountDenom:
        type: string
      priceDenom:
        type: string
      book:
        type: object
        properties:
          idCount:
            type: integer
            format: int32
          orders:
            type: array
            items:
              type: object
              properties:
                id:
                  type: integer
                  format: int32
                creator:
                  type: string
                amount:
                  type: integer
                  format: int32
                price:
                  type: integer
                  format: int32
  lightmos.restaking.ValidatorToken:
    type: object
    properties:
      address:
        type: string
      total:
        type: string
      retire:
        type: string
      available:
        type: string
